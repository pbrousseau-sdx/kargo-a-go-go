apiVersion: v1
kind: Service
metadata:
  name: {{ include "plat.fullname" . }}
  labels:
    {{- include "plat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plat.selectorLabels" . | nindent 4 }}
