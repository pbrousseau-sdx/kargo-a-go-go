apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plat.fullname" . }}-test-connection"
  labels:
    {{- include "plat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
